#!/usr/bin/env bash
# a script that modify localhost and facebok addresses
cp /etc/hosts /etc/host.backup
sudo sed -i 'localhost' /etc/hosts
sudo sed -i 'facebook.com' /etc/hosts
sudo tee -a '127.0.0.2     localhost' /etc/hosts
sudo tee -a '8.8.8.8       facebook.com' /etc/hosts