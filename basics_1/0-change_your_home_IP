#!/usr/bin/env bash
# a script that modify localhost and facebok addresses
sudo sed -i '/localhost/c\127.0.0.2     localhost' /etc/hosts
sudo grep -qxF '8.8.8.8       facebook.com' /etc/hosts || echo '8.8.8.8       facebook.com' >> /etc/hosts